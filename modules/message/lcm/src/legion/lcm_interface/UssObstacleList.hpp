/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_UssObstacleList_hpp__
#define __lcm_interface_UssObstacleList_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/UssObstacle.hpp"

namespace lcm_interface
{

/// UssObstacleList::UssObstacleList
class UssObstacleList
{
    public:
        lcm_interface::Header header;

        /// 消息头
        int32_t    uss_obstacles_size;

        std::vector< lcm_interface::UssObstacle > uss_obstacles;

        /// 检测出的超声波障碍物数组
        int32_t    uss_ranges_size;

        std::vector< double > uss_ranges;

        /// 超声波检测的距离数组，存放顺序按照FA,FB,FC,FD,FE,FF,RA,RB,RC,RD,RE,RF
        int32_t    error_code;

        /// 错误码（default = OK）
        int8_t     is_valid;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "UssObstacleList"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int UssObstacleList::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int UssObstacleList::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int UssObstacleList::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t UssObstacleList::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* UssObstacleList::getTypeName()
{
    return "UssObstacleList";
}

int UssObstacleList::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->uss_obstacles_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->uss_obstacles_size; a0++) {
        tlen = this->uss_obstacles[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->uss_ranges_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->uss_ranges_size > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->uss_ranges[0], this->uss_ranges_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->error_code, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->is_valid, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int UssObstacleList::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->uss_obstacles_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->uss_obstacles.resize(this->uss_obstacles_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->uss_obstacles_size; a0++) {
        tlen = this->uss_obstacles[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->uss_ranges_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->uss_ranges_size) {
        this->uss_ranges.resize(this->uss_ranges_size);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->uss_ranges[0], this->uss_ranges_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->error_code, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->is_valid, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int UssObstacleList::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->uss_obstacles_size; a0++) {
        enc_size += this->uss_obstacles[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->uss_ranges_size);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t UssObstacleList::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == UssObstacleList::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, UssObstacleList::getHash };

    uint64_t hash = 0xa5ea0a6229721836LL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::UssObstacle::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
