/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_AlarmMessage_hpp__
#define __lcm_interface_AlarmMessage_hpp__

#include <lcm/lcm_coretypes.h>

#include <string>
#include "lcm_interface/Header.hpp"

namespace lcm_interface
{

/// AlarmMessage::AlarmMessage
class AlarmMessage
{
    public:
        lcm_interface::Header header;

        /// 消息头
        std::string alarm_proc;

        /// 产生告警的进程名
        int32_t    alarm_level;

        /// 告警等级
        int32_t    alarm_type;

        /// 告警类型
        int32_t    alarm_id;

        /// 告警序列号，用于恢复告警
        int32_t    alarm_display;

        /// 告警展示类型
        std::string alarm_data;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "AlarmMessage"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int AlarmMessage::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int AlarmMessage::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int AlarmMessage::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t AlarmMessage::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* AlarmMessage::getTypeName()
{
    return "AlarmMessage";
}

int AlarmMessage::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    char* alarm_proc_cstr = const_cast<char*>(this->alarm_proc.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &alarm_proc_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->alarm_level, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->alarm_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->alarm_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->alarm_display, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* alarm_data_cstr = const_cast<char*>(this->alarm_data.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &alarm_data_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int AlarmMessage::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __alarm_proc_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__alarm_proc_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__alarm_proc_len__ > maxlen - pos) return -1;
    this->alarm_proc.assign(
        static_cast<const char*>(buf) + offset + pos, __alarm_proc_len__ - 1);
    pos += __alarm_proc_len__;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->alarm_level, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->alarm_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->alarm_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->alarm_display, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __alarm_data_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__alarm_data_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__alarm_data_len__ > maxlen - pos) return -1;
    this->alarm_data.assign(
        static_cast<const char*>(buf) + offset + pos, __alarm_data_len__ - 1);
    pos += __alarm_data_len__;

    return pos;
}

int AlarmMessage::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->alarm_proc.size() + 4 + 1;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->alarm_data.size() + 4 + 1;
    return enc_size;
}

uint64_t AlarmMessage::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == AlarmMessage::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, AlarmMessage::getHash };

    uint64_t hash = 0xd3e993d1f93eeff6LL +
         lcm_interface::Header::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
