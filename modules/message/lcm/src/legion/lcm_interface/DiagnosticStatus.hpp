/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_DiagnosticStatus_hpp__
#define __lcm_interface_DiagnosticStatus_hpp__

#include <lcm/lcm_coretypes.h>

#include <string>
#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/KeyValues.hpp"

namespace lcm_interface
{

/// DiagnosticStatus::DiagnosticStatus
class DiagnosticStatus
{
    public:
        lcm_interface::Header header;

        /// timestamp is  included in header
        int32_t    id;

        /// level*100000 + 序号（RECOVER 400000） WARN 车运动状态（静止）100001 左前门状态（关闭）100002 右前门状态（关闭）100003 左后门状态（关闭）100004 右后门状态（关闭）100005 安全带状态（关闭）100006 后视镜状态（打开）100007 充电枪状态 100008 油门状态 （正常）100009 智驾系统（正常）100010 动力系统（正常）100011 左前轮胎压（正常）100012 右前轮胎压（正常）100013 左后轮胎压（正常）100014 右后轮胎压（正常）100015 转向系统（正常）100016 制动系统（正常）100017 档位系统（正常）100018 EPB系统（正常）100019 气囊状态（正常）100020 油箱盖状态（正常）100021 后备箱状态（正常）100022 底盘信息（正常）100023 定位信息（正常）100024 方向盘人工接管 100025 电量表（正常）100026 暂停超时 100027 暂停次数过多 100028 泊车超时 100029 人为刹车 100030 人为挂P挡 100031 障碍物碰撞 100032 EPB状态(释放) 100033 ERROR 规划失败 200000
        int32_t    level;

        /// 诊断状态
        std::string name;

        /// 测试/组件报告的描述
        std::string message;

        /// 状态描述
        std::string hardware_id;

        /// 硬件的唯一描述
        int32_t    values_size;

        std::vector< lcm_interface::KeyValues > values;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "DiagnosticStatus"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int DiagnosticStatus::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int DiagnosticStatus::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int DiagnosticStatus::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t DiagnosticStatus::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* DiagnosticStatus::getTypeName()
{
    return "DiagnosticStatus";
}

int DiagnosticStatus::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->level, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* name_cstr = const_cast<char*>(this->name.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &name_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* message_cstr = const_cast<char*>(this->message.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &message_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* hardware_id_cstr = const_cast<char*>(this->hardware_id.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &hardware_id_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->values_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->values_size; a0++) {
        tlen = this->values[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int DiagnosticStatus::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->level, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __name_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__name_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__name_len__ > maxlen - pos) return -1;
    this->name.assign(
        static_cast<const char*>(buf) + offset + pos, __name_len__ - 1);
    pos += __name_len__;

    int32_t __message_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__message_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__message_len__ > maxlen - pos) return -1;
    this->message.assign(
        static_cast<const char*>(buf) + offset + pos, __message_len__ - 1);
    pos += __message_len__;

    int32_t __hardware_id_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__hardware_id_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__hardware_id_len__ > maxlen - pos) return -1;
    this->hardware_id.assign(
        static_cast<const char*>(buf) + offset + pos, __hardware_id_len__ - 1);
    pos += __hardware_id_len__;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->values_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->values.resize(this->values_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->values_size; a0++) {
        tlen = this->values[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int DiagnosticStatus::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->name.size() + 4 + 1;
    enc_size += this->message.size() + 4 + 1;
    enc_size += this->hardware_id.size() + 4 + 1;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->values_size; a0++) {
        enc_size += this->values[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t DiagnosticStatus::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == DiagnosticStatus::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, DiagnosticStatus::getHash };

    uint64_t hash = 0xb3bdb72e6a192762LL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::KeyValues::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
