/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_TrajectoryArray_hpp__
#define __lcm_interface_TrajectoryArray_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/Path.hpp"
#include "lcm_interface/Path.hpp"
#include "lcm_interface/Trajectory.hpp"

namespace lcm_interface
{

/// TrajectoryArray::TrajectoryArray
class TrajectoryArray
{
    public:
        lcm_interface::Header header;

        /**
         */
        int32_t    spline_s_size;

        std::vector< lcm_interface::Path > spline_s;

        /// 样条拟合曲线
        int32_t    qp_smooth_size;

        std::vector< lcm_interface::Path > qp_smooth;

        /// qp参考线拟合
        int32_t    trajectory_list_size;

        std::vector< lcm_interface::Trajectory > trajectory_list;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "TrajectoryArray"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int TrajectoryArray::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int TrajectoryArray::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int TrajectoryArray::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t TrajectoryArray::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* TrajectoryArray::getTypeName()
{
    return "TrajectoryArray";
}

int TrajectoryArray::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->spline_s_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->spline_s_size; a0++) {
        tlen = this->spline_s[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->qp_smooth_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->qp_smooth_size; a0++) {
        tlen = this->qp_smooth[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->trajectory_list_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->trajectory_list_size; a0++) {
        tlen = this->trajectory_list[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int TrajectoryArray::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->spline_s_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->spline_s.resize(this->spline_s_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->spline_s_size; a0++) {
        tlen = this->spline_s[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->qp_smooth_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->qp_smooth.resize(this->qp_smooth_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->qp_smooth_size; a0++) {
        tlen = this->qp_smooth[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->trajectory_list_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->trajectory_list.resize(this->trajectory_list_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->trajectory_list_size; a0++) {
        tlen = this->trajectory_list[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int TrajectoryArray::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->spline_s_size; a0++) {
        enc_size += this->spline_s[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->qp_smooth_size; a0++) {
        enc_size += this->qp_smooth[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->trajectory_list_size; a0++) {
        enc_size += this->trajectory_list[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t TrajectoryArray::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == TrajectoryArray::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, TrajectoryArray::getHash };

    uint64_t hash = 0x6f71ca405a3961e6LL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::Path::_computeHash(&cp) +
         lcm_interface::Path::_computeHash(&cp) +
         lcm_interface::Trajectory::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
