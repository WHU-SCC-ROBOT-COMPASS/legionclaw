/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_LaneList_hpp__
#define __lcm_interface_LaneList_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/SensorCalibrator.hpp"
#include "lcm_interface/LaneLine.hpp"
#include "lcm_interface/HolisticPathPrediction.hpp"
#include "lcm_interface/RoadMark.hpp"

namespace lcm_interface
{

/// LaneList::LaneList
class LaneList
{
    public:
        lcm_interface::Header header;

        /// 消息头
        int32_t    sensor_id;

        /// 安装的传感器id(camera,lidar,radar) ' 相机id: 0-front_center相机 1-front_left相机 2-front_right相机 3-left_front相机 4-left_back右后相机 5-right_front相机 6-right_back相机 7-back相机'  8-相机融合
        int8_t     error_code;

        /// 错误码： ERROR_NONE = 0; ERROR_UNKNOWN = 1;
        int8_t     sensor_status;

        /// 传感器状态： NORMAL=0； ABNORMAL=1；
        int32_t    change_origin_flag;

        /// 坐标切换状态（0-坐标系切换成功 1-坐标系切换中 2-坐标系切换故障）
        int8_t     is_valid;

        /// 车道线数据是否合法
        lcm_interface::SensorCalibrator sensor_calibrator;

        /// 传感器标定参数
        int32_t    camera_laneline_size;

        std::vector< lcm_interface::LaneLine > camera_laneline;

        /// 车道线检测结果数组
        lcm_interface::HolisticPathPrediction hpp;

        /// 行驶预测线
        int32_t    road_marks_size;

        std::vector< lcm_interface::RoadMark > road_marks;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "LaneList"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int LaneList::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LaneList::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LaneList::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t LaneList::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* LaneList::getTypeName()
{
    return "LaneList";
}

int LaneList::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->sensor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->error_code, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->sensor_status, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->change_origin_flag, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->is_valid, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->sensor_calibrator._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->camera_laneline_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->camera_laneline_size; a0++) {
        tlen = this->camera_laneline[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->hpp._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->road_marks_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->road_marks_size; a0++) {
        tlen = this->road_marks[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int LaneList::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->sensor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->error_code, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->sensor_status, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->change_origin_flag, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->is_valid, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->sensor_calibrator._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->camera_laneline_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->camera_laneline.resize(this->camera_laneline_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->camera_laneline_size; a0++) {
        tlen = this->camera_laneline[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->hpp._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->road_marks_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->road_marks.resize(this->road_marks_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->road_marks_size; a0++) {
        tlen = this->road_marks[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int LaneList::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += this->sensor_calibrator._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->camera_laneline_size; a0++) {
        enc_size += this->camera_laneline[a0]._getEncodedSizeNoHash();
    }
    enc_size += this->hpp._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->road_marks_size; a0++) {
        enc_size += this->road_marks[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t LaneList::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == LaneList::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, LaneList::getHash };

    uint64_t hash = 0x785ff30666957b05LL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::SensorCalibrator::_computeHash(&cp) +
         lcm_interface::LaneLine::_computeHash(&cp) +
         lcm_interface::HolisticPathPrediction::_computeHash(&cp) +
         lcm_interface::RoadMark::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
