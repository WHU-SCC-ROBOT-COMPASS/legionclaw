/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_LaneLine_hpp__
#define __lcm_interface_LaneLine_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/LaneLineCubicCurve.hpp"
#include "lcm_interface/LaneLineCubicCurve.hpp"
#include "lcm_interface/LaneLineCubicCurve.hpp"
#include "lcm_interface/Point3D.hpp"
#include "lcm_interface/Point2D.hpp"
#include "lcm_interface/Point3D.hpp"
#include "lcm_interface/EndPoints.hpp"
#include "lcm_interface/Point2D.hpp"
#include "lcm_interface/Time.hpp"

namespace lcm_interface
{

/// LaneLine::LaneLine
class LaneLine
{
    public:
        int32_t    lane_type;

        /// 车道线类型：   SOLID = 0;   DASHED = 1;   ROAD_DELIMITER = 2;   NONE = 3;   UNKNOWN = 4;
        int32_t    lane_color;

        /// 车道线颜色：   WHITE = 0;   YELLOW = 1;   OTHERS = 2;   UNKNOWN = 3;
        int32_t    pos_type;

        /// 车道线位置类别：    BOLLARD_LEFT = -5;   FOURTH_LEFT = -4;   THIRD_LEFT = -3;   ADJACENT_LEFT = -2;  //!< lane marking on the left side next to ego lane   EGO_LEFT = -1;       //!< left lane marking of the ego lane   EGO_RIGHT = 1;       //!< right lane marking of the ego lane   ADJACENT_RIGHT = 2;  //!< lane marking on the right side next to ego lane   THIRD_RIGHT = 3;   FOURTH_RIGHT = 4;   BOLLARD_RIGHT = 5;   OTHER = 6;    //!< other types of lane   UNKNOWN = 7;  //!< background
        lcm_interface::LaneLineCubicCurve curve_vehicle;

        /// 车辆坐标系车道线三次曲线系数
        lcm_interface::LaneLineCubicCurve curve_image;

        /// 图像坐标系车道线三次曲线系数
        lcm_interface::LaneLineCubicCurve curve_abs;

        /// 世界坐标系车道线三次曲线系数
        int32_t    pts_vehicle_size;

        std::vector< lcm_interface::Point3D > pts_vehicle;

        /// 车辆坐标系车道线点集
        int32_t    pts_image_size;

        std::vector< lcm_interface::Point2D > pts_image;

        /// 图像坐标系车道线点集
        int32_t    pts_abs_size;

        std::vector< lcm_interface::Point3D > pts_abs;

        /// 世界坐标系车道线点集
        lcm_interface::EndPoints image_end_point;

        /// 车道线上顶点与下顶点
        int32_t    pts_key_size;

        std::vector< lcm_interface::Point2D > pts_key;

        /// 车道线关键点数组
        int8_t     hd_lane_id;

        /// 高精车道线id
        double     confidence;

        /// 车道线置信度
        int32_t    lane_quality;

        /// 车道线质量：   VERY_LOW = 0;   LOW = 1;   HIGH = 2;   VERY_HIGH = 3;
        int32_t    fused_lane_type;

        /// 融合车道线类别：   CAMERA = 0;   HD = 1;   FUSED = 2;   UNKNOWN = 3;   if value is 1, lane_quality = 3
        int32_t    homography_mat_size;

        std::vector< double > homography_mat;

        /// 透视变换矩阵
        int32_t    homography_mat_inv_size;

        std::vector< double > homography_mat_inv;

        /// 透视变换逆矩阵
        int32_t    lane_coordinate_type;

        /// 坐标系类别：   FRAME = 0;   IMAGE = 1;   CAMERA = 2;   VEHICLE = 3;   ABSOLUTE = 4;
        int32_t    use_type;

        /// 使用类别：   REAL = 0;   VIRTUAL = 1;
        lcm_interface::Time create_time;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "LaneLine"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int LaneLine::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LaneLine::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LaneLine::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t LaneLine::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* LaneLine::getTypeName()
{
    return "LaneLine";
}

int LaneLine::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lane_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lane_color, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->pos_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_vehicle._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_image._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_abs._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->pts_vehicle_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->pts_vehicle_size; a0++) {
        tlen = this->pts_vehicle[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->pts_image_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->pts_image_size; a0++) {
        tlen = this->pts_image[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->pts_abs_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->pts_abs_size; a0++) {
        tlen = this->pts_abs[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->image_end_point._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->pts_key_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->pts_key_size; a0++) {
        tlen = this->pts_key[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->hd_lane_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->confidence, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lane_quality, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->fused_lane_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->homography_mat_size > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->homography_mat[0], this->homography_mat_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_inv_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->homography_mat_inv_size > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_inv[0], this->homography_mat_inv_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lane_coordinate_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->use_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->create_time._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LaneLine::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lane_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lane_color, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->pos_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_vehicle._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_image._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->curve_abs._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->pts_vehicle_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->pts_vehicle.resize(this->pts_vehicle_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->pts_vehicle_size; a0++) {
        tlen = this->pts_vehicle[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->pts_image_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->pts_image.resize(this->pts_image_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->pts_image_size; a0++) {
        tlen = this->pts_image[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->pts_abs_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->pts_abs.resize(this->pts_abs_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->pts_abs_size; a0++) {
        tlen = this->pts_abs[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->image_end_point._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->pts_key_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->pts_key.resize(this->pts_key_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->pts_key_size; a0++) {
        tlen = this->pts_key[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->hd_lane_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->confidence, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lane_quality, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->fused_lane_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->homography_mat_size) {
        this->homography_mat.resize(this->homography_mat_size);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->homography_mat[0], this->homography_mat_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_inv_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->homography_mat_inv_size) {
        this->homography_mat_inv.resize(this->homography_mat_inv_size);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->homography_mat_inv[0], this->homography_mat_inv_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lane_coordinate_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->use_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->create_time._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LaneLine::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->curve_vehicle._getEncodedSizeNoHash();
    enc_size += this->curve_image._getEncodedSizeNoHash();
    enc_size += this->curve_abs._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->pts_vehicle_size; a0++) {
        enc_size += this->pts_vehicle[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->pts_image_size; a0++) {
        enc_size += this->pts_image[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->pts_abs_size; a0++) {
        enc_size += this->pts_abs[a0]._getEncodedSizeNoHash();
    }
    enc_size += this->image_end_point._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->pts_key_size; a0++) {
        enc_size += this->pts_key[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->homography_mat_size);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->homography_mat_inv_size);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->create_time._getEncodedSizeNoHash();
    return enc_size;
}

uint64_t LaneLine::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == LaneLine::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, LaneLine::getHash };

    uint64_t hash = 0xd44d45eade9eb4fdLL +
         lcm_interface::LaneLineCubicCurve::_computeHash(&cp) +
         lcm_interface::LaneLineCubicCurve::_computeHash(&cp) +
         lcm_interface::LaneLineCubicCurve::_computeHash(&cp) +
         lcm_interface::Point3D::_computeHash(&cp) +
         lcm_interface::Point2D::_computeHash(&cp) +
         lcm_interface::Point3D::_computeHash(&cp) +
         lcm_interface::EndPoints::_computeHash(&cp) +
         lcm_interface::Point2D::_computeHash(&cp) +
         lcm_interface::Time::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
