/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_PredictionObstacle_hpp__
#define __lcm_interface_PredictionObstacle_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/PerceptionObstacle.hpp"
#include "lcm_interface/TrajectoryInPrediction.hpp"
#include "lcm_interface/ObstacleIntent.hpp"
#include "lcm_interface/ObstaclePriority.hpp"
#include "lcm_interface/ObstacleInteractiveTag.hpp"

namespace lcm_interface
{

/// PredictionObstacle::PredictionObstacle
class PredictionObstacle
{
    public:
        lcm_interface::PerceptionObstacle perception_obstacle;

        /// perception info of obstacle
        double     timestamp;

        /// GPS time in seconds
        double     predicted_period;

        /// the length of the time for this prediction (e.g. 10s)
        int32_t    trajectory_size;

        std::vector< lcm_interface::TrajectoryInPrediction > trajectory;

        /// can have multiple trajectories per obstacle
        lcm_interface::ObstacleIntent intent;

        /// 估计障碍物的意图        0-UNKNOWN, 1-STOP, 2-STATIONARY, 3-移动 MOVING, 4-HANGE_LANE, 5-LOW_ACCELERATION, 6-HIGH_ACCELERATION, 7-LOW_DECELERATION, 8-HIGH_DECELERATION, 
        lcm_interface::ObstaclePriority priority;

        /// 障碍物优先级               1-CAUTION, 2-NORMAL, 3-IGNORE
        lcm_interface::ObstacleInteractiveTag interactive_tag;

        /**
         */
        int8_t     is_static;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "PredictionObstacle"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int PredictionObstacle::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PredictionObstacle::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int PredictionObstacle::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t PredictionObstacle::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* PredictionObstacle::getTypeName()
{
    return "PredictionObstacle";
}

int PredictionObstacle::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->perception_obstacle._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->predicted_period, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->trajectory_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->trajectory_size; a0++) {
        tlen = this->trajectory[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->intent._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->priority._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->interactive_tag._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->is_static, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PredictionObstacle::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->perception_obstacle._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->predicted_period, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->trajectory_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->trajectory.resize(this->trajectory_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->trajectory_size; a0++) {
        tlen = this->trajectory[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = this->intent._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->priority._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->interactive_tag._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->is_static, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PredictionObstacle::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->perception_obstacle._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->trajectory_size; a0++) {
        enc_size += this->trajectory[a0]._getEncodedSizeNoHash();
    }
    enc_size += this->intent._getEncodedSizeNoHash();
    enc_size += this->priority._getEncodedSizeNoHash();
    enc_size += this->interactive_tag._getEncodedSizeNoHash();
    enc_size += __boolean_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t PredictionObstacle::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == PredictionObstacle::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, PredictionObstacle::getHash };

    uint64_t hash = 0xc704e5795e5a8d32LL +
         lcm_interface::PerceptionObstacle::_computeHash(&cp) +
         lcm_interface::TrajectoryInPrediction::_computeHash(&cp) +
         lcm_interface::ObstacleIntent::_computeHash(&cp) +
         lcm_interface::ObstaclePriority::_computeHash(&cp) +
         lcm_interface::ObstacleInteractiveTag::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
