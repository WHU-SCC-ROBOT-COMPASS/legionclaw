/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_RoutingRequest_hpp__
#define __lcm_interface_RoutingRequest_hpp__

#include <lcm/lcm_coretypes.h>

#include <string>
#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/KeyPoint.hpp"

namespace lcm_interface
{

/// RoutingRequest::RoutingRequest
class RoutingRequest
{
    public:
        lcm_interface::Header header;

        /// timestamp is included in header
        std::string request_source;

        /// 发起请求的来源名称
        int8_t     request_type;

        /// REQUEST_TYPE=0//无效 REQUEST_TYPE=1//正常路由 REQUEST_TYPE=2//泊车路由
        int16_t    num_of_kp;

        /// 关键点数量
        int32_t    key_point_list_size;

        std::vector< lcm_interface::KeyPoint > key_point_list;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "RoutingRequest"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int RoutingRequest::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int RoutingRequest::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int RoutingRequest::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t RoutingRequest::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* RoutingRequest::getTypeName()
{
    return "RoutingRequest";
}

int RoutingRequest::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    char* request_source_cstr = const_cast<char*>(this->request_source.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &request_source_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->request_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_of_kp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->key_point_list_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->key_point_list_size; a0++) {
        tlen = this->key_point_list[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int RoutingRequest::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __request_source_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__request_source_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__request_source_len__ > maxlen - pos) return -1;
    this->request_source.assign(
        static_cast<const char*>(buf) + offset + pos, __request_source_len__ - 1);
    pos += __request_source_len__;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->request_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_of_kp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->key_point_list_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->key_point_list.resize(this->key_point_list_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->key_point_list_size; a0++) {
        tlen = this->key_point_list[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int RoutingRequest::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->request_source.size() + 4 + 1;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->key_point_list_size; a0++) {
        enc_size += this->key_point_list[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t RoutingRequest::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == RoutingRequest::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, RoutingRequest::getHash };

    uint64_t hash = 0xc5a43ded22ceeb3fLL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::KeyPoint::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
