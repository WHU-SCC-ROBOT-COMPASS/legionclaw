/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_LaneInfoExtend_hpp__
#define __lcm_interface_LaneInfoExtend_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/LanePoint.hpp"

namespace lcm_interface
{

/// LaneInfoExtend::LaneInfoExtend
class LaneInfoExtend
{
    public:
        int8_t     priority;

        /// 道路优先级
        int64_t    global_id;

        /// 道路全局id（从左至右，从0开始）
        int32_t    predecessor_ids_size;

        std::vector< int64_t > predecessor_ids;

        /// 所有的前继车道id数组
        int32_t    successor_ids_size;

        std::vector< int64_t > successor_ids;

        /// 所有的后续车道id数组
        int64_t    left_neighbor_id;

        /// 左边相邻车道id
        int64_t    right_neighbor_id;

        /// 右边相邻车道id
        int8_t     type;

        /// 类型（预留）
        int32_t    lane_points_size;

        std::vector< lcm_interface::LanePoint > lane_points;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "LaneInfoExtend"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int LaneInfoExtend::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int LaneInfoExtend::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LaneInfoExtend::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t LaneInfoExtend::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* LaneInfoExtend::getTypeName()
{
    return "LaneInfoExtend";
}

int LaneInfoExtend::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->priority, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->global_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->predecessor_ids_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->predecessor_ids_size > 0) {
        tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->predecessor_ids[0], this->predecessor_ids_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->successor_ids_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->successor_ids_size > 0) {
        tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->successor_ids[0], this->successor_ids_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->left_neighbor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->right_neighbor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lane_points_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->lane_points_size; a0++) {
        tlen = this->lane_points[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int LaneInfoExtend::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->priority, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->global_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->predecessor_ids_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->predecessor_ids_size) {
        this->predecessor_ids.resize(this->predecessor_ids_size);
        tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->predecessor_ids[0], this->predecessor_ids_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->successor_ids_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->successor_ids_size) {
        this->successor_ids.resize(this->successor_ids_size);
        tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->successor_ids[0], this->successor_ids_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->left_neighbor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->right_neighbor_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lane_points_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->lane_points.resize(this->lane_points_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->lane_points_size; a0++) {
        tlen = this->lane_points[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int LaneInfoExtend::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, this->predecessor_ids_size);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, this->successor_ids_size);
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->lane_points_size; a0++) {
        enc_size += this->lane_points[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t LaneInfoExtend::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == LaneInfoExtend::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, LaneInfoExtend::getHash };

    uint64_t hash = 0x80b0b970fe73e52fLL +
         lcm_interface::LanePoint::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
