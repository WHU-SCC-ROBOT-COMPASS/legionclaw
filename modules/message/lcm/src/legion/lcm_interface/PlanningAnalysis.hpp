/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_PlanningAnalysis_hpp__
#define __lcm_interface_PlanningAnalysis_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/TimeConsume.hpp"
#include "lcm_interface/PlanningParkingDebug.hpp"

namespace lcm_interface
{

/// PlanningAnalysis::PlanningAnalysis
class PlanningAnalysis
{
    public:
        lcm_interface::Header header;

        /**
         */
        double     frame_update_time;

        /// frame update计算时间
        double     generator_time;

        /// 轨迹生成 计算时间
        int32_t    evaluator_time_size;

        std::vector< lcm_interface::TimeConsume > evaluator_time;

        /// 轨迹评估计算时间
        double     selector_time;

        /// 轨迹选择计算时间
        lcm_interface::PlanningParkingDebug planning_parking_debug;

        /// planning泊车调试消息
        double     dis_to_center_line;

        /// 车辆距离车道中心线的垂直距离
        double     diff_to_velocity;

        /// 目标速度与规划速度之差
        double     referencline_kappa;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "PlanningAnalysis"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int PlanningAnalysis::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PlanningAnalysis::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int PlanningAnalysis::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t PlanningAnalysis::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* PlanningAnalysis::getTypeName()
{
    return "PlanningAnalysis";
}

int PlanningAnalysis::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->frame_update_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->generator_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->evaluator_time_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->evaluator_time_size; a0++) {
        tlen = this->evaluator_time[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->selector_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->planning_parking_debug._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->dis_to_center_line, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->diff_to_velocity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->referencline_kappa, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PlanningAnalysis::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->frame_update_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->generator_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->evaluator_time_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->evaluator_time.resize(this->evaluator_time_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->evaluator_time_size; a0++) {
        tlen = this->evaluator_time[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->selector_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->planning_parking_debug._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->dis_to_center_line, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->diff_to_velocity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->referencline_kappa, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int PlanningAnalysis::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->evaluator_time_size; a0++) {
        enc_size += this->evaluator_time[a0]._getEncodedSizeNoHash();
    }
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += this->planning_parking_debug._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t PlanningAnalysis::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == PlanningAnalysis::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, PlanningAnalysis::getHash };

    uint64_t hash = 0x3802d93a302585c9LL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::TimeConsume::_computeHash(&cp) +
         lcm_interface::PlanningParkingDebug::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
