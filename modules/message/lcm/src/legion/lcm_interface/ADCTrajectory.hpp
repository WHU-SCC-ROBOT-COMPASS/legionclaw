/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __lcm_interface_ADCTrajectory_hpp__
#define __lcm_interface_ADCTrajectory_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include <string>
#include "lcm_interface/Header.hpp"
#include "lcm_interface/TrajectoryPoint.hpp"
#include "lcm_interface/EStop.hpp"
#include "lcm_interface/RSSInfo.hpp"

namespace lcm_interface
{

/// ADCTrajectory::ADCTrajectory
class ADCTrajectory
{
    public:
        lcm_interface::Header header;

        /// timestamp is included in header
        double     total_path_length;

        /// unit meters(m)
        double     total_path_time;

        /// unit seconds(s)
        int32_t    trajectory_points_size;

        std::vector< lcm_interface::TrajectoryPoint > trajectory_points;

        /// 规划轨迹点列
        int32_t    car_action;

        /// 车辆行为： CAR_ACTION_NONE=0  //无效值 CAR_ACTION_SINGLE=1 //单车 CAR_ACTION_PLATOON_HEAD=2 //车队头车 CAR_ACTION_PLATOON_OTHER=3 //车队其他车辆
        int32_t    behaviour_lat_state;

        /// 自动驾驶状态（主要指横向状态）
        int32_t    behaviour_lon_state;

        /// 自动驾驶状态（主要指纵向状态）
        int32_t    scenario;

        /// 驾驶场景 0-UNKNOWN, 1-CRUISE, 2-CRUISE_URBAN 3-CRUISE_HIGHWAY, 2000-JUNCTION  2001-JUNCTION_TRAFFIC_LIGHT 2002-JUNCTION_STOP_SIGN
        int32_t    driving_mode;

        /// 驾驶模式： 1：人工驾驶， 2：辅助驾驶， 3：自动驾驶
        int32_t    adc_trajectory_type;

        /// 轨迹类型： TRAJECTORY_TYPE_ABSOLUTE=0 //绝对规划轨迹 TRAJECTORY_TYPE_RELATIVE=1 //相对规划轨迹 TRAJECTORY_TYPE_INVALID=2 //无效规划轨迹
        lcm_interface::EStop estop;

        /// emergency stop
        int8_t     is_replan;

        /// is_replan == truemean replan triggered(default = false)
        std::string replan_reason;

        /**
         */
        int32_t    right_of_way_status;

        /// UNPROTECTED = 0;PROTECTED = 1;
        lcm_interface::RSSInfo rss_info;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ADCTrajectory"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int ADCTrajectory::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int ADCTrajectory::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ADCTrajectory::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ADCTrajectory::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* ADCTrajectory::getTypeName()
{
    return "ADCTrajectory";
}

int ADCTrajectory::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->total_path_length, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->total_path_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->trajectory_points_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->trajectory_points_size; a0++) {
        tlen = this->trajectory_points[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->car_action, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->behaviour_lat_state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->behaviour_lon_state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->scenario, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->driving_mode, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->adc_trajectory_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->estop._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->is_replan, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* replan_reason_cstr = const_cast<char*>(this->replan_reason.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &replan_reason_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->right_of_way_status, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->rss_info._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int ADCTrajectory::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->total_path_length, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->total_path_time, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->trajectory_points_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->trajectory_points.resize(this->trajectory_points_size);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->trajectory_points_size; a0++) {
        tlen = this->trajectory_points[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->car_action, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->behaviour_lat_state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->behaviour_lon_state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->scenario, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->driving_mode, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->adc_trajectory_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->estop._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->is_replan, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __replan_reason_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__replan_reason_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__replan_reason_len__ > maxlen - pos) return -1;
    this->replan_reason.assign(
        static_cast<const char*>(buf) + offset + pos, __replan_reason_len__ - 1);
    pos += __replan_reason_len__;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->right_of_way_status, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->rss_info._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int ADCTrajectory::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->trajectory_points_size; a0++) {
        enc_size += this->trajectory_points[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->estop._getEncodedSizeNoHash();
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += this->replan_reason.size() + 4 + 1;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->rss_info._getEncodedSizeNoHash();
    return enc_size;
}

uint64_t ADCTrajectory::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == ADCTrajectory::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, ADCTrajectory::getHash };

    uint64_t hash = 0x7f720c08bc06030aLL +
         lcm_interface::Header::_computeHash(&cp) +
         lcm_interface::TrajectoryPoint::_computeHash(&cp) +
         lcm_interface::EStop::_computeHash(&cp) +
         lcm_interface::RSSInfo::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
